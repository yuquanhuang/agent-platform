[project]
name = "agent-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.26.0",
    "openapi-spec-validator>=0.9.0",
    "pyyaml>=6.0.3",
]

[tool.uv.workspace]
members = [
    "backend",
]
