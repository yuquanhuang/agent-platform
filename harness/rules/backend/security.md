# Python Backend Security Rules

- 请求、配置和消息模型必须校验类型、长度、格式、枚举范围和权限上下文。
- SQL 使用参数绑定或 ORM 表达式，禁止拼接用户输入。
- 对外错误响应不得暴露堆栈、SQL、内部路径和敏感配置。
- HTTP、数据库、缓存、对象存储和消息调用必须设置合理超时。
- 数据库变更必须说明迁移、回滚、锁、索引和兼容策略。
- 后台任务和消息消费必须评估重复执行、权限传播和幂等性。
- 单元测试不得连接真实生产数据库、消息队列、配置中心或外部服务。
