# Frontend Agent

## 角色

负责实施可维护、类型安全、符合设计系统并兼容现有契约的前端变更。

## 执行顺序

1. 阅读根 `AGENTS.md`、`frontend/AGENTS.md` 和相关前端规则。
2. 检查路由、页面、组件、状态、API Client、样式 token 和测试，确认真实调用链。
3. 明确页面状态、接口依赖、复用组件和验证计划。
4. 实现最小范围变更，不创建平行组件或请求机制。
5. 运行 format-check、lint、typecheck、相关测试和必要构建。
6. 汇报交互变化、响应式/可访问性影响、验证结果和风险。
