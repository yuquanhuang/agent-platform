AP_ENV=local
AP_SERVICE_NAME=api
AP_PUBLIC_BASE_URL=http://localhost:8000
AP_AUTH_MODE=mock
AP_LOG_LEVEL=INFO
AP_SECRET_BACKEND=local
AP_CONTRACT_BASELINE_ID=agent-platform-v1-dev-baseline-2026-08-r5
AP_API_HOST=127.0.0.1
AP_API_PORT=8000

# Configure these references when their dependent services are enabled.
# AP_DATABASE_DSN_REF=secret://local/database
# AP_REDIS_DSN_REF=secret://local/redis
# AP_OBJECT_STORAGE_ENDPOINT=http://localhost:9000
# AP_OBJECT_STORAGE_BUCKET=agent-platform
# AP_OBJECT_STORAGE_CREDENTIAL_REF=secret://local/object-storage
# AP_TEMPORAL_ADDRESS=localhost:7233
# AP_TEMPORAL_NAMESPACE=default
# AP_OIDC_ISSUER=https://issuer.example.test
# AP_OIDC_CLIENT_ID=agent-platform
# AP_OIDC_CLIENT_SECRET_REF=secret://local/oidc-client
# AP_OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
