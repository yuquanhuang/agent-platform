apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: agent-platform-sandbox-default-deny
  namespace: agent-platform
spec:
  podSelector:
    matchLabels:
      agent-platform.io/sandbox: "true"
  policyTypes: [Ingress, Egress]
  ingress: []
  egress: []
